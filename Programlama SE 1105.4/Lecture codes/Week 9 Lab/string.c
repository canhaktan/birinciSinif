#include <stdio.h>

int main() {

    /* char my_name[] = {'e', 'l', 'i', 'f'}; // WRONG
    char my_name[] = {'e', 'l', 'i', 'f', '\0'}; // CORRECT
    printf("%s\n", my_name); */

    /* char my_name[] = "elif";
    printf("%s\n", my_name); */

    /* char my_string[100];
    for(int i = 0; i < 5; i++) {
        scanf(" %c", &my_string[i]);     // Space before %c makes sure that newlines wont be counted as chars
    }
    my_string[5] = '\0';
    printf("%s\n", my_string); */

    /* char my_string[5];
    printf("Write a string no longer than 5 chars: ");
    scanf("%s", my_string);
    printf("Your string: %s\n", my_string); */

    /* char my_string[100];
    printf("Write a string no longer than 100 chars: ");
    scanf("%s", my_string);
    int i = 0;
    while(my_string[i] != '\0') {
        printf("%c ", my_string[i]);
        i++;
    }
    printf("\n"); */
    
    return 0;
}